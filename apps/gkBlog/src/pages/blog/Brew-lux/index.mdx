---
title: "Brew lux 跨平台视频下载工具-支持所有视频平台"
description: "Lux是一个使用Go语言编写的视频下载命令行工具"
date: "2025-06-30"
lang: zh
tags:
  - 编程
  - 工具
category: 技术
cover: https://boost.jpgcdn.com/t3.jpgcdn.com/2025/06/30/sMht.png
---

import Image from "@/components/mdx/Image";

<Image
  src="https://boost.jpgcdn.com/t3.jpgcdn.com/2025/06/30/sMht.png"
  alt="Brew lux 跨平台视频下载工具-支持所有视频平台"
  width={999}
  height={527}
  isArticleImage={true}
/>

## Brew lux 跨平台视频下载工具-支持所有视频平台

Lux 是一个使用 Go 语言编写的视频下载命令行工具，支持的平台很多，提供了包括 macOS、Windows、Linux 等等平台的命令行支持，安装和使用非常简单的。Lux 原来的名字是叫做 Annie（安妮），对标的是 macOS 上一款非常著名的视频下载软件叫做 Downie（唐尼）。但后来改名成了 Lux。

Lux 支持非常多的视频网站：抖音、哔哩哔哩、半次元、pixivision、优酷、YouTube、爱奇艺、芒果 TV、Tumblr、Vimeo、Facebook、斗鱼视频、秒拍、新浪微博、Instagram、Twitter、腾讯视频、网易云音乐、音悦台...

项目 ：https://github.com/iawia002/lux

### 一、安装

macOS 下：

```bash
brew install ffmpegbrew install lux
```

或者从 Release 页面下载，手动安装:

```bash
https://github.com/iawia002/lux/releases
```

```bash
curl “https://github.com/iawia002/lux/releases/download/v0.15.0/lux_0.15.0_macOS_64-bit.tar.gz”
```

### 二、使用

直接下载
Lux 的使用很简单，在终端：

```bash
lux “https://www.youtube.com/watch?v=QJXPS0gQ6Eg"
```

```bash
lux “https://www.bilibili.com/video/BV1ZJ411h713"
```

视频链接给的是播放列表里的第二个视频。如果用 youtube-dl 下载会自作主张地下载播放列表的第一个视频。在对付B站播放列表方面，Annie比较乖一点。

Annie不仅可以下载视频，如果给的是图片的网址，它就下载图片。给播放列表的链接就下载播放列表。

### 三、下载不同分辨率

首先使用 -i 参数查看可供下载的视频分辨率， -i 参数可以一次添加多个视频链接，空格分隔。

```bash
lux -i “https://www.bilibili.com/video/BV1ZJ411h713"
```

然后使用 -f 来指定要下载的分辨率：

```bash
lux -f 64-7 [https://www.bilibili.com/video/BV1ZJ411h713](https://www.bilibili.com/video/BV1ZJ411h713)
```

### 四、下载列表

使用 -p 来下载视频列表。

如果要指定列表中的位置，可以使用如下几个选项：

-start 从视频列表的第几个开始下
-end 下载到几个
-items 指定要下载哪几个，比如 1,5,6,8-10
-eto B 站独有的参数，用于没有标题只有文件名的播放列表
举例：

```bash
lux -p -start 1 -end 15 “https://www.bilibili.com/video/BV1no4y1C7oo"
```

### 五、从文件中读取下载链接

可以使用 -F 参数，从包含视频 URL 的文本文件中下载视频：

```bash
lux -F /path/to/links.txt
```

这里也可以用参数-start、-end和-items。

按住 ctrl+c 可以中断下载，可以续传。

B 站上可以用 av 和 ep 加数字下载视频，比如：

```bash
annie -i ep198381 av21877586
```

### 六、其余参数

可以使用 -o 参数后面接路径，来指定将视频保存到该路径下：

```bash
lux -o ~/Videos/ URL
```

使用 -O NAME 来指定输出的名字。

输出 JSON
可以使用 -j 输出 JSON 格式结果。
